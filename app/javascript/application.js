// Entry point for the build script in your package.json
import "@hotwired/turbo-rails"
import "./controllers"
import "@hotwired/turbo-rails"

// importa o Popper obrigatoriamente antes do Bootstrap
import "@popperjs/core"

// importa o JS do Bootstrap
import "bootstrap"