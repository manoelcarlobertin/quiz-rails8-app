<%# content_for :title, "Editar Quiz" %>

<div class="container mt-4">
  <h1 class="text-primary mb-4">Editar Quiz</h1>

  <div class="card">
    <div class="card-body">
      <%= render "form", perguntum: @perguntum %>
    </div>
  </div>

  <div class="mt-4">
    <%= link_to "Visualizar Quiz", @perguntum, class: "btn btn-info me-2" %>
    <%= link_to "Voltar para Listagem", pergunta_path, class: "btn btn-secondary" %>
  </div>
</div>