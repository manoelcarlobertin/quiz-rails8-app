<p class="alert alert-success"><%= notice %></p>

<% content_for :title, "Quizzes" %>

<h1 class="text-primary mb-4">Listagem de Quizzes</h1>

<div class="container">
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>Título</th>
        <th>Descrição</th>
        <th>Ações</th>
      </tr>
    </thead>
    <tbody>
      <% @quizzes.each do |quiz| %>
        <tr>
          <td><%= quiz.titulo %></td>
          <td><%= quiz.descricao %></td>
          <td>
            <%= link_to "Alterar", edit_quiz_path(quiz), class: "btn btn-warning btn-sm" %>
            <%= button_to "Excluir", quiz, class: "btn btn-danger btn-sm", method: :delete, data: { confirm: "Tem certeza?" } %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<div class="mt-3">
  <%= link_to "Novo Quiz", new_quiz_path, class: "btn btn-primary" %>
</div>
